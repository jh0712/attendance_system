<div class="form-group form_label">
    @if($label)
        <label class="form-label">{!! $label !!}</label>
    @endif
    @if($value)
    <div class="form-value">
        {!! $value !!}
    </div>
    @endif
</div>